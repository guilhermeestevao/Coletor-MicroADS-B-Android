<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/dialog_remote_settings_head"
        android:textSize="20sp"
        android:textStyle="bold"
        android:padding="13dp"
        android:background="@color/accent"
        android:text="Configurações de conexão remota"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/white" />


    <RelativeLayout
        android:id="@+id/middle_layout"
        android:layout_below="@id/dialog_remote_settings_head"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <LinearLayout
            android:id="@+id/dialog_update_password_form"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginTop="20dp"
            android:layout_marginLeft="20dp"
            android:layout_marginRight="20dp"
            android:focusable="true"
            android:focusableInTouchMode="true">

            <EditText
                android:id="@+id/ip_server"
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
                android:hint="IP"
                android:inputType="text" />

            <EditText
                android:id="@+id/port_server"
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
                android:hint="Porta"
                android:inputType="number" />

        </LinearLayout>

    </RelativeLayout>

</RelativeLayout>